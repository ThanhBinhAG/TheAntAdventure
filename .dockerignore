# Dependencies & build output
node_modules
.next
out
build
*.tsbuildinfo

# Git / IDE / local-only
.git
.gitignore
.cursor
.vscode
.idea
Personal
Material
Material_test
backups

# Env & secrets (inject at runtime / build-arg)
.env
.env.local
.env.*
!.env.example

# Tests and documentation are verifier inputs but are not copied into runner.
coverage

# CI verifier inputs are required by source-contract tests but are not copied
# into the production runner stage.
.npm
npm-debug.log*
yarn-error.log*
.dockerignore
