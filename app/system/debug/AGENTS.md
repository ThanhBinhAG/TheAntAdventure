# app/system/debug/ — Agent overview

## Role
Token-gated debug page wrapping `components/system/DebugPanel`.

## Contents
- Page + thin client wrapper

## Boundaries
- Heavy UI stays in `components/system`; APIs under `app/api/system`.
