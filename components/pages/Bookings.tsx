/**
 * Page wrapper for URL /bookings.
 * Booking UI lives in components/bookings/BookingsPage.
 */
import BookingsPage from '@/components/bookings/BookingsPage';

export default function Bookings() {
  return <BookingsPage />;
}
