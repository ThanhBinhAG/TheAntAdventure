/**
 * Page wrapper for URL /contracts.
 * UI lives in components/contracts/ContractsPage.tsx.
 */
import ContractsPage from '@/components/contracts/ContractsPage';

export default function Contracts() {
  return <ContractsPage />;
}
