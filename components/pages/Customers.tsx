/**
 * Page wrapper for URL /customers.
 * UI lives in components/customers/CustomersPage.tsx.
 */
import CustomersPage from '@/components/customers/CustomersPage';

export default function Customers() {
  return <CustomersPage />;
}
