/**
 * Page wrapper for URL /gallery.
 * Heavy UI lives in components/gallery/GalleryWorkspace.
 */
import GalleryWorkspace from '@/components/gallery/GalleryWorkspace';

export default function Gallery() {
  return <GalleryWorkspace />;
}
