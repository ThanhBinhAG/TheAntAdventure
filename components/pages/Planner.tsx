/**
 * Page wrapper for URL /planner.
 * Detail UI lives in components/planner/PlannerPage.
 */

import PlannerPage from '@/components/planner/PlannerPage';

export default function Planner() {
  return <PlannerPage />;
}
