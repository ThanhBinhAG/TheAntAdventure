/**
 * Page wrapper for URL /settings.
 * Thin shell — UI lives in components/settings.
 */
import SettingsPage from '@/components/settings/SettingsPage';

export default function Settings() {
  return <SettingsPage />;
}
