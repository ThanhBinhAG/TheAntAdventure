# components/salary/ — Agent overview

## Role
Salary payroll UI (tabs: payroll, structure, bonus).

## Contents
- `SalaryPage.tsx` — payroll table and export

## Boundaries
- Data via `useSalaryPage` and `/api/salary`; no browser Supabase.
