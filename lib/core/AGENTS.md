# lib/core/ — Agent overview

## Role
Shared CRM/UI utilities (dates, money, print, page helpers).

## Contents
- `crm-utils.ts`, `date-utils.ts`, `money.ts`, `page-helpers.ts`, `print-window.ts`, …

## Boundaries
- Domain-specific logic belongs in the matching `lib/<domain>/` folder.
