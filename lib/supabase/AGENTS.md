# lib/supabase/ — Agent overview

## Role
Browser Supabase client factory and Next middleware helpers.

## Contents
- `client.ts`, `index.ts`, `middleware.ts`

## Boundaries
- Hydrate/push: `lib/db`. React sync context: `lib/context`.
