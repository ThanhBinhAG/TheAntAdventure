# supabase/legacy/ — Agent overview

## Role
Archive note only: pre-CLI `migrate-*.sql` patches were squashed into the v5 baseline migration.

## Contents
- `LEGACY-MIGRATIONS.md` — status + ops file index

## Boundaries
- Do not add new SQL patches here. Use `../migrations/` via `npm run db:migration:new`.
